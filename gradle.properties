org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true
kotlin.code.style=official

# The Gradle daemon must run on a JDK the Kotlin/Compose toolchain supports.
# The JDK on PATH here is 26; JDK 21 is already provisioned under ~/.gradle/jdks.
# Gradle uses the configured JDK; JDK 17 or 21 is recommended for AGP.
# if you prefer to run ./gradlew directly.
